{
  "name": "telnyx-cypress-cucumber",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/support/step_definitions/**/*.js"
    ],
    "json": {
      "enabled": true,
      "output": "cypress/cucumber-json/report.json"
    }
  },
  "scripts": {
    "cy:run": "cypress run",
    "cy:run:mobile": "cypress run --config-file cypress.config.mobile.js",
    "report": "node generate-report.mjs",
    "test": "npm run cy:run && npm run report"
  },
  "keywords": [],
  "author": "Ernest Sosnovskyi",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^28.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.8",
    "cypress": "^16.0.0",
    "multiple-cucumber-html-reporter": "^4.3.0"
  }
}